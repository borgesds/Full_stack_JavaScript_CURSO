class transportSpaceship extends spaceship {
    constructor(nome, crewQuantity, sitsQuantity) {
        super(nome, crewQuantity) //construtor da spaceship
        this.sitsQuantity = sitsQuantity
    }
}